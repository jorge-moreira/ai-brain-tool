{
  "name": "ai-brain-tool-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "cli:start": "bun run --cwd packages/cli start",
    "cli:test": "bun run --cwd packages/cli test",
    "electron:dev": "bun run --cwd packages/electron dev",
    "electron:build": "bun run --cwd packages/electron build",
    "ui:build": "bun run --cwd packages/ui build",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@commitlint/cli": "^20.5.2",
    "@commitlint/config-conventional": "^20.5.0"
  },
  "devDependencies": {
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/npm": "^13.1.5",
    "@semantic-release/release-notes-generator": "^14.1.0",
    "semantic-release": "^25.0.3"
  }
}
