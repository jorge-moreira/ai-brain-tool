{
  "name": "@jorge-moreira.dev/ai-brain-tool",
  "version": "2.0.1",
  "description": "Your personal AI memory, connected to all your AI tools",
  "type": "module",
  "bin": {
    "ai-brain": "bin/ai-brain.js"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.0"
  },
  "scripts": {
    "test": "vitest run tests/unit tests/integration",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:e2e": "vitest run tests/e2e",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage tests/unit tests/integration",
    "test:coverage:bun": "bun test:coverage",
    "test:coverage:ci": "vitest run --coverage --reporter=github-actions tests/unit tests/integration",
    "test:coverage:ci:bun": "bun test:coverage:ci",
    "start": "node bin/ai-brain.js",
    "start:bun": "bun run bin/ai-brain.js",
    "prepare": "husky"
  },
  "files": [
    "bin/",
    "src/",
    "requirements.txt"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "dependencies": {
    "@inquirer/prompts": "^8.4.2",
    "chalk": "^5.0.0",
    "commander": "^14.0.3",
    "execa": "^9.0.0",
    "ora": "^9.4.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^12.0.6",
    "@semantic-release/npm": "^13.1.5",
    "@semantic-release/release-notes-generator": "^14.1.0",
    "@vitest/coverage-v8": "^4.1.5",
    "husky": "^9.1.7",
    "semantic-release": "^25.0.3",
    "vitest": "^4.1.5"
  }
}